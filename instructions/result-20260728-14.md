# instruction-20260728-14 作業結果報告

## 対象

- 指示書: `instructions/instruction-20260728-14.md`
- 承認済み設計: `docs/superpowers/specs/2026-07-28-canon-finalization-mail-lineage-design.md`
- 開始HEAD: `3451341 docs: clarify canon recovery and mail idempotency`
- 開始ブランチ: `main`、`origin/main`と同期、作業ツリーclean
- 残存PID: 2924、35956はいずれも存在せず、30秒CPU確認も空。別作業の活動なし。

## 実装済み範囲

- `canon_publications`を追加し、`BEGIN IMMEDIATE`で作品・版・セッション単位の予約を実装。
- 5成果物を`.staging/{publication_id}`へ生成・検証し、版ディレクトリ単位のrenameで公開。
- rename後の再開、DB INSERT途中のロールバック後再開、完了済みDBのメール再試行を同じ版で処理。
- 最終ディレクトリの不完全・メタデータ不一致、staging/final同時存在時は削除・上書き・新版予約をせず診断エラーにした。
- 5件の`canon_documents`、publication、session、works更新を単一SQLiteトランザクションで確定。
- AgentMailに一意`idempotency_key`を追加し、既存メールを再利用する送信を実装。
- 監査完了、承認、却下、再監査、scene-planner引き継ぎで親ID・conversation ID・hop countを継承。
- 却下理由と修正指示パスを含め、旧草案・監査を削除せずrevision継続。

## 変更ファイル

- `kobo/canon.py`
- `mail/agent_mail.py`
- `tests/test_canon.py`
- `mail/test_agent_mail.py`
- `instructions/result-20260728-14.md`

## テスト・検証

- `py -3 -m unittest tests.test_canon -v`: 7件成功
- `py -3 -m unittest mail.test_agent_mail -v`: 24件成功、1件スキップ（Windows権限依存）
- `py -3 -m unittest discover -v`: 124件成功、1件スキップ、失敗0
- `py -3 -m compileall -q kobo mail tests`: 成功
- `git diff --check`: 成功
- 障害注入・境界確認: DB INSERT途中のトランザクションロールバック、rename後の同一版再開、不完全finalの非破壊停止、冪等メール再送をテスト。
- 実Gemini、外部ネットワーク、有料API: 未実施（dummy adapterのみ）。

## 未解決事項

- 実ファイルシステム障害そのもの（電源断・ディスク障害）の実機検証は未実施。決定的なテスト注入点は実装済み。

## コミット・push

- コミットSHA: `6c42196 fix: make canon finalization resumable`（結果報告更新コミットは後続）
- push結果: push前

## 最終状態

- ブランチ: `main`
- `git status --short --branch`: コミット後にclean確認
- 次のエージェントは最初に`git status --short --branch`、`git log --oneline --decorate -3`、`git show --stat HEAD`を実行する。
