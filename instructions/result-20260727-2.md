# `instruction-20260727-2.md` 実行結果

## 1. 結果

`instructions/instruction-20260727-2.md`に従い、再利用可能なAIエージェント間メール通信を`SPEC.md`へ統合した。

親Claude Codeを子AI間の通常会話の伝言経路から外し、親が担当する範囲を作業分割、重要判断、最終統合、例外対応に限定した。子AI間の質問、回答、レビュー依頼、修正確認は共有SQLiteメールボックスで直接交換する仕様とした。

## 2. 変更したファイル

- `SPEC.md`
- `instructions/result-20260727-2.md`

指定外ファイルは変更していない。

## 3. `SPEC.md`の更新内容

- 版を0.2から0.3へ更新し、版履歴を追加した。
- 第3章へ、親による全文中継がコンテキストとトークンを消費する背景を追加した。
- 第4章・第5章へ、通信基盤の適用範囲と対象外を追加した。
- 第6章へ、AIエージェント間メール、未処理項目、エスカレーションの用語を追加した。
- 第7章へ、親Claude Codeと子AIの通信上の責務を追加した。
- 第11章へ、メール処理フロー、親へ戻す情報、CLI・Python APIの操作境界を追加した。
- 第12章へ、エージェント情報、メッセージ項目、双方の既読状態、自己完結配置、Git除外を追加した。
- 第15章へ、親コンテキスト負荷、SQLite整合性、決定的順序、移植性、回復性、DB保護を追加した。
- 第16章・第18章へ、不正操作の拒否と機密情報保護を追加した。
- 第19章へMVP範囲と段階2の高度なワーカー制御を追加した。
- 第20章へμITRON型イベント駆動ワーカーと高度なキュー機能を将来拡張として追加した。
- 第21章へ受入条件AC-017・AC-018を追加した。
- 第22章へTBD-010を追加した。
- 第23章の要求トレーサビリティと件数を更新した。

## 4. 追加した要求IDと受入条件ID

追加した要求IDは26件。

- `ROLE-009`〜`ROLE-010`
- `FR-063`〜`FR-070`
- `NFR-013`〜`NFR-018`
- `DATA-013`〜`DATA-016`
- `AI-011`〜`AI-014`
- `SAFE-011`〜`SAFE-012`

追加した受入条件は2件。

- `AC-017` コピー可能なAIエージェント間メールの一往復
- `AC-018` 親Claude Codeを通常連絡の中継から外す

要求IDは合計136件から162件、受入条件は16件から18件になった。

## 5. 親Claude Codeの負荷削減

- 子AI間の通常連絡を親へ全文中継しない。
- 親はメール本文を定期的に全件取り込まない。
- 親へ戻すのは最終成果、重大問題、権限不足、処理不能、全体仕様の判断が必要な事項だけとした。
- エスカレーションは論点、選択肢、推奨案、影響の短い要約とした。
- トークン削減率は未計測のため、根拠のない数値目標を設けなかった。

## 6. コピー再利用性

- 実装、README、AI向け起動指示、使用例、テスト、Git除外設定を`mail/`内にまとめる要求とした。
- `mail/`を別プロジェクトへコピーし、DB初期化と任意のAIエージェント登録だけで利用開始できる受入条件を設けた。
- Python標準ライブラリだけで動作することを要求した。
- 小説工房固有の役割名をコードへ固定せず、任意の登録済み`agent_id`を使えることを要求した。
- 既定DBを`mail/agent_mail.db`とし、DB、WAL、PythonキャッシュをGit追跡対象から外すことを要求した。

## 7. 実装と仕様の差異

現行`mail/agent_mail.py`は、次を実装している。

- DB初期化
- AIエージェント登録
- 新規メール送信
- 未読件数取得
- 未処理項目の決定的な時系列取得
- 返信と受信者既読の同一トランザクション更新
- 送信者による返信既読化
- 不正な返信、二重返信、二重既読化の拒否
- WALモード、30秒のロック待ち
- JSON形式のCLI応答

次は未実装であり、現行機能として記載せず、段階2・将来拡張・TBD-010へ分離した。

- メール到着でAIを起動し、処理後に再待機させるμITRON型ワーカーループ
- `pending`／`processing`／`completed`／`failed`状態
- 複数ワーカー間の原子的な処理予約
- 異常終了後の再試行とタイムアウト
- 優先度、スレッド、ブロードキャスト、添付ファイル
- 複数ホスト間のSQLite DB同期

## 8. 未決事項

`TBD-010`として次を追加した。

- メール往復の上限
- 最大本文長
- 保存期間
- アーカイブ・削除方法
- 同時利用数
- ワーカーの処理予約
- 再試行回数
- タイムアウト値

推奨は、現行の一件処理方式で実測し、段階2で確定すること。

## 9. 検証結果

- `python -m unittest -v mail/test_agent_mail.py`: 5件成功
- 要求ID: 162件。全系列で重複・欠番なし
- 受入条件ID: AC-001〜AC-018。重複・欠番なし
- Markdown表: 列数不一致なし
- コードフェンス: 開閉一致
- `git diff --check`: エラーなし
- 変更対象: `SPEC.md`と`instructions/result-20260727-2.md`のみ

## 10. 作業完了時のGit状態

コミット前の確認状態:

```text
## main...origin/main
 M SPEC.md
?? instructions/result-20260727-2.md
```

この結果報告作成後、指定対象だけをコミットし、`main`へプッシュする。
