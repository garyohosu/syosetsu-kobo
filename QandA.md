# QandA — `SPEC.md`作成時の確認事項（全16問 回答済み）

| 項目 | 内容 |
|---|---|
| 状態 | **全16問 回答済み** |
| 回答の出典 | Q1〜Q6: `instructions/instruction-20260727-3.md`、Q7〜Q16: `instructions/instruction-20260727-5.md`（hantaniの正式回答） |
| 反映先 | `SPEC.md` v0.7、`mail/`実装、オーケストレーター、README、テスト |
| 提起時の対象 | `SPEC.md` v0.1およびv0.4レビュー（2026-07-27） |

`SPEC.md` v0.1の作成時に、hantaniの判断が必要な事項6件をこのファイルへ整理した。さらにv0.4レビューで確認した10件を追加した。
16問すべてに正式回答が示され、Q7〜Q16は`instructions/instruction-20260727-5.md`に基づき`SPEC.md` v0.5と実装へ反映する。

本ファイルは回答済みの決定記録である。以後、ここに書かれた事項を未決事項として扱わない。

`instruction-20260727-7.md`のGemini CLI接続と対話型URS実装では、既存回答と仕様から一意に実装できない新たな製品判断は発生しなかったため、Q17以降は追加していない。TBD-004〜TBD-006とTBD-010の未解決部分も正式決定していない。

`instruction-20260727-8.md`の企画候補工程も、候補数を設定可能な仮仕様（既定3、許容1〜5）として実装でき、新たな利用者判断を必要としなかったため質問は追加していない。AI推奨は利用者選択の代替にせず、TBD-004〜TBD-006とTBD-010を解決済みにしていない。

2026-07-27のストーリーバイブル・全体プロット工程も、既存仕様の責務分離、独立監査、明示承認、非上書き確定を適用でき、新たな利用者判断を必要としなかった。TBD-004〜TBD-006とTBD-010は引き続き未解決である。

同日の章・シーン設計および本文制作工程は、既定の承認単位を章として実装し、初稿・監査・部分改稿・差分再監査を別成果物にした。停止条件の正式値は定めず1回の改稿を初期実装としたため、TBD-004〜TBD-006とTBD-010は未解決のままである。

- 各質問には、提起時の選択肢、AIの仮判断、**hantaniの正式回答**を分けて記載する。AIの仮判断は当時の推奨であり、決定ではない。
- 提起時の「今は決めない」という選択肢は履歴として残すが、いずれの質問でも採用されていない。
- 決定の詳細と反映先の要求IDは`SPEC.md`第22.2節「解決済みの決定記録」にある。
- 未解決の事項は本ファイル末尾および`SPEC.md`第22.3節を参照する。Q7〜Q16の回答によって、TBD-010のうち保存期間・アーカイブ・削除時期以外の実装値は確定した。

## 決定一覧

| 質問 | 対応TBD | 採用 | 決定の要点 | 主な反映先 |
|---|---|---|---|---|
| Q1 | `TBD-001` | 選択肢1 | 対話相手は工房長（内部）。外部壁打ちログは任意入力 | FR-001、FR-059、SAFE-009、DATA-004 |
| Q2 | `TBD-007` | 選択肢4（既定は選択肢1） | 承認単位は既定「話（節）」、`URS.md`で上書き可 | FR-061、SAFE-010、STATE-002、DATA-003、DATA-005 |
| Q3 | `TBD-009` | 選択肢1 | `MyLike_kousatsu.md`10項目を初期質問キューへ | FR-060、FR-018、SAFE-005 |
| Q4 | `TBD-002` | 選択肢1＋非依存性の付帯条件 | MVPはClaude Code中心。役割定義と通信は最初から非依存 | AI-010、NFR-010、LOG-001 |
| Q5 | （TBD番号なし） | 選択肢1 | 短い試作は段階2。FR-022とAC-013は保持 | 第19.3節、FR-022、AC-013 |
| Q6 | `TBD-008` | 選択肢1 | MVPは単一アクティブ作品。作品IDは保持 | FR-062、DATA-002、第4章 |
| Q7 | `TBD-010` | 選択肢1 | 待機ポーリング型ワーカーをv0.5 MVPへ正式採用 | NFR-017、第20章、AC-019〜AC-025 |
| Q8 | — | 選択肢3 | 処理完了・既読・返信済みを別概念として維持 | STATE-010、AC-019〜AC-020 |
| Q9 | `TBD-010` | 選択肢1 | `max_attempts`は初回を含む合計試行回数。既定3。`max_retries`は互換名 | NFR-020、AC-020 |
| Q10 | — | 選択肢2 | CLI子プロセスは強制終了保証、Pythonハンドラーは協調停止範囲 | NFR-021、NFR-022、AC-020 |
| Q11 | — | 選択肢2 | リース・フェンシングでDB更新を保護し、副作用は冪等性で担保 | NFR-017、NFR-022、AC-021 |
| Q12 | — | 選択肢3 | 未登録対象は安全な既定値で自動登録。既存属性は上書きしない | FR-071、AC-024 |
| Q13 | — | 選択肢1 | 親メールから系列情報を導出し、矛盾を拒否 | DATA-017、AC-022 |
| Q14 | `TBD-010` | 選択肢2 | 本文長・新規DB権限を実装。保存運用の具体値は未決 | NFR-023、AC-025 |
| Q15 | — | 選択肢2 | 正式名称は待機ポーリング型ワーカー | 用語、第20章 |
| Q16 | — | 選択肢1 | AC-019〜AC-025を正式MVP受入条件として全章へ反映 | 第19・21・23・24章 |

---

## Q1. 対話型URSメーカーの対話相手は誰か（`TBD-001`）

`memo.md`§16は「ChatGPTとの壁打ちによって`URS.md`を作成する」と書いています。
一方で§5.1は工房長をClaude Codeとし、§16.1・§16.7は対話型URSメーカーを小説工房の内部工程として定義しています。
どちらを正とするかで、MVPの実装範囲と`URS.md`の入出力経路が変わります。

**提起時の選択肢:**

1. どちらでも開始でき、外部の壁打ちログは任意入力として受け付ける（AIの推奨）
2. Claude Code内で完結させる
3. 外部ChatGPTとの壁打ちを必須工程にする
4. 今は決めない
5. 自由記入

**AIの仮判断（提起時。決定ではない）:** `memo.md`§16.1〜§16.7の記述はすべてシステム内部工程として書かれており、「ChatGPTとの壁打ち」は着想の由来を述べた一文と読むのが自然。選択肢1を推奨。

### 正式回答: 選択肢1を採用

- MVPでは小説工房の内部、すなわち工房長であるClaude Codeとの対話によって`URS.md`を作成する。
- ChatGPTなど外部AIとの壁打ちは必須にしない。
- 外部で行った壁打ちログ、メモ、既存回答は任意入力として取り込める。
- 取り込んだ情報は出典を区別し、既に回答済みの質問を省略するために利用する。
- 外部ログを取り込まなくても、全工程を開始・完了できる。
- 外部ログを無条件に正しい要求として確定せず、必要に応じてhantaniへ確認する。

**反映先:** `SPEC.md` FR-001（対話相手の確定）、FR-059（任意取り込み）、SAFE-009（無条件確定の禁止）、DATA-004（出典併記と既定ラベル）、AC-001、AC-002、第19.2節、第22.2節。

---

## Q2. hantaniが承認する単位はどこか（`TBD-007`）

`memo.md`§4.7は「hantaniが承認した後」とだけ書いており、承認の粒度が決まっていませんでした。
これが決まらないと、状態遷移（`SPEC.md` STATE-002）と台帳更新のタイミング（FR-046）を実装できません。

**提起時の選択肢:**

1. 話（節）単位（AIの推奨）
2. 場面単位
3. 章単位
4. 作品ごとに`URS.md`で指定する
5. 今は決めない
6. 自由記入

**AIの仮判断（提起時。決定ではない）:** 既定を選択肢1にし、選択肢4の上書きを許す形が、負担最小化（GOAL-002）と柔軟性の両立になる。

### 正式回答: 選択肢4を採用し、既定値は選択肢1の「話（節）単位」とする

- 既定の承認単位は「話（節）」とする。
- 作品ごとに`URS.md`で、場面単位、話単位、章単位などへ上書きできる。
- `URS.md`に指定がない場合は話単位を使用する。
- 承認対象の単位が完成するまでは案または承認待ちであり、正史へ確定しない。
- hantaniの明示的な承認後にだけ、対象単位を正史化し、台帳と嗜好モデルを更新する。
- 承認単位を途中で変更する場合は、`URS.md`の改訂として変更理由と適用開始位置を記録する。
- 過去に承認済みの単位へ、変更後の粒度を黙って遡及適用しない。

**反映先:** `SPEC.md` FR-061（承認単位の決定規則）、FR-046（承認単位での台帳更新）、SAFE-010（遡及適用の禁止）、STATE-002・STATE-003（適用単位）、DATA-003、DATA-005（適用開始位置）、AC-008、AC-015、第22.2節。

---

## Q3. `MyLike.md`の26作品について、どこが好きかをどう聞き取るか（`TBD-009`）

`MyLike.md`には作品名だけが記録されています。
`MyLike_kousatsu.md`も「作品名だけを材料にした分析」と自ら断っており、10項目を「現段階では断定できないこと」として挙げています。
そのため、嗜好モデルが`AI推定`ばかりになり、実際の好みとずれたまま企画が作られる恐れがありました。

**提起時の選択肢:**

1. 対話型URSメーカーの初期質問キューへ組み込む（AIの推奨）
2. 事前に別途まとめて聞き取る
3. 推定のまま進め、企画案への反応で検証する
4. 今は決めない
5. 自由記入

**AIの仮判断（提起時。決定ではない）:** 選択肢1を推奨。`MyLike_kousatsu.md`の10質問は既に選択式へ変換しやすい形になっている。

### 正式回答: 選択肢1を採用

- `MyLike_kousatsu.md`にある「URS作成時に優先して尋ねる質問」10項目を、対話型URSメーカーの初期質問キューへ組み込む。
- 一度に10問をまとめて提示せず、一問ずつ、選択肢、読み味や影響、推奨案、「今は決めない」、自由記入を含む既存の対話原則に従って聞く。
- 最初に「特に好きな上位5作品」を選んでもらい、各作品について好きな点を順次確認する。
- 26作品すべてについて、最初から詳細回答を必須にしない。
- 既に分かっている情報や過去の回答は再質問しない。
- `MyLike_kousatsu.md`の分析は`AI推定`または`未確認仮説`として保持し、hantaniの回答で確認・修正された場合だけ確度を上げる。
- 「今は決めない」が選ばれた項目は創作開始を妨げず、作品案への反応から後で更新できる。

**反映先:** `SPEC.md` FR-060（初期質問キュー）、FR-018、FR-005（重複質問の省略）、SAFE-005、AC-001、AC-003、第22.2節。

---

## Q4. 個別AIエージェントに使うモデル・CLIをどうするか（`TBD-002`）

`memo.md`§11-3が未決事項として明記していました。エージェント人格プロンプトのテンプレート作成に必要です。

**提起時の選択肢:**

1. MVPはClaude Codeのサブエージェントで統一し、段階2で他モデル混在を検証する（AIの推奨）
2. 最初から役割ごとに他社モデルを混在させる
3. 今は決めない
4. 自由記入

**AIの仮判断（提起時。決定ではない）:** 選択肢1を推奨。モデル非依存性は「役割定義の粒度」で担保でき、実際の混在検証は後段でも間に合う。

### 正式回答: 選択肢1を採用。ただし通信と役割定義は最初からモデル・CLI非依存にする

実装段階の選択（MVP）:

- MVPの最初の動作確認はClaude Codeおよびそのサブエージェントを基本とする。
- MVP完了条件に、Claude、Codex、Gemini、Grokなど複数ベンダーの混在動作を含めない。
- 段階2で、他モデル・他CLIとの混在を検証する。

設計上の非依存性（MVPから適用）:

- エージェントの役割定義、入出力ファイル、`agent_id`、結果形式に、Claude Code固有の会話機能を必須条件として埋め込まない。
- 子AI間の通常連絡を親Claude Codeが全文中継する構成を必須にしない。
- `instructions/instruction-20260727-2.md`で追加予定のSQLiteメール通信と矛盾せず、将来は独立したAI CLIをエージェントとして接続できる境界を保つ。
- 使用したモデル、CLI、版、役割は実行記録へ残す。

この決定は「当面Claude Codeで検証する」という実装段階の選択であって、Claude Codeへの恒久的な製品依存を意味しない。

**反映先:** `SPEC.md` AI-010（実装段階の選択と設計上の非依存性の分離）、NFR-010、LOG-001（モデル・CLI・版・役割の記録）、第19.3節、第22.2節。

---

## Q5. MVPで「短い試作」工程を必須にするか（`SPEC.md` §19.3・FR-022）

`memo.md`§4.3は、長編を書く前に冒頭や重要場面を数種類試作して方向を決める工程を挙げています。
一方、`memo.md`§11-8の最小構成は「企画案生成→プロット監査→改訂→第1話試作」で、独立した試作工程を必須にしていません。
`SPEC.md` v0.1ではこれを段階2へ置きましたが、判断の分かれる箇所でした。

この質問はTBD番号を持ちません（`SPEC.md` v0.1では段階分けの判断事項として扱っていました）。

**提起時の選択肢:**

1. 段階2へ回す（AIの推奨・v0.1の記述）
2. MVPに含める
3. 今は決めない
4. 自由記入

**AIの仮判断（提起時。決定ではない）:** 選択肢1を推奨。ただし嗜好モデルの初期精度を重視するなら選択肢2にも合理性がある。

### 正式回答: 選択肢1を採用

- 独立した複数バリアントの短い試作工程は段階2へ置く。
- MVPでは第1話の初稿提示を方向確認として利用する。
- `FR-022`および`AC-013`自体を削除せず、段階2の要求・受入条件として保持する。
- MVPでも、hantaniが明示的に依頼した場合に試作を行うことは妨げない。ただしMVP完了の必須条件にはしない。

**反映先:** `SPEC.md` 第19.3節（段階2への配置理由と明示依頼時の扱い）、FR-022、AC-013、第23.3節。

---

## Q6. 複数作品を並行して進めるか（`TBD-008`）

`memo.md`§5.3は`projects/<work-id>/`というディレクトリ構成を想定していますが、複数作品の同時進行についての要求は書かれていません。
これが決まると、継続指示（「続けて」）で「どの作品の続きか」を解決する仕組みが必要かどうかが決まります。

**提起時の選択肢:**

1. MVPは単一作品のみ。ディレクトリは`<work-id>`を持つ形にして後から拡張できるようにする（AIの推奨）
2. 最初から複数作品に対応する
3. 今は決めない
4. 自由記入

**AIの仮判断（提起時。決定ではない）:** 選択肢1を推奨。2作目に入る前に決めれば間に合う。

### 正式回答: 選択肢1を採用

- MVPで同時にアクティブに扱う作品は1作品だけとする。
- 保存構造は最初から`projects/<work-id>/`とし、作品を識別できる形を維持する。
- 「続けて」「続きを書いて」など作品IDを省略した継続指示は、MVPでは唯一のアクティブ作品へ適用する。
- 複数作品の同時進行、アクティブ作品の切替、曖昧な継続指示の解決は段階2以降とする。
- 将来複数作品へ拡張できるよう、作品IDをデータやパスから削除しない。

**反映先:** `SPEC.md` FR-062（アクティブ作品の特定と作品IDの保持）、DATA-002、第4章（適用範囲）、第12.2節（作品ID階層の確定）、第19.3節、第20章、AC-016、第22.2節。

---

## 未解決の事項

次の4件と、`TBD-010`の保存運用部分は**未解決のまま**である。
詳細（何が未決定か、なぜ決定が必要か、選択肢、推奨案、推奨理由、先送りの影響）は`SPEC.md`第22.3節にある。

いずれもhantaniの判断を必須とせず、実装時または段階2の実測で決められるため、本ファイルには質問として立てていない。

| ID | 内容 | 状態 | 決め方 |
|---|---|---|---|
| `TBD-003` | 実行ID・ファイル命名・状態の保存形式 | 未解決 | 実装時に決定（推奨案: 日時＋工程名＋連番） |
| `TBD-004` | 改善ループの停止条件の確定値 | 未解決 | MVPは「最大3周」を`仮仕様`で運用し、段階2で実測して確定 |
| `TBD-005` | 確信度の尺度 | 未解決 | 実装時に決定（推奨案: 高／中／低の3段階） |
| `TBD-006` | 非機能要件の定量目標 | 未解決 | `memo.md`に根拠がないため設定せず、段階2で実測値からベースラインを設定 |
| `TBD-010`（未解決部分） | メール保存期間、アーカイブ時期、削除時期 | 未解決 | 運用実測後に決定。本文長、再試行、タイムアウト、ホップ制限、DB権限はQ7〜Q16で決定済み |

これらについて先に決めたい場合は、その旨を指示してください。

## 参考: 元資料について気づいた点

`instructions/instruction-20260727-1.md` §12および`instructions/instruction-20260727-3.md` §7に従い、`memo.md`、`MyLike.md`、`MyLike_kousatsu.md`は一切変更していません。
気づいた点は`SPEC.md`第22.1節へ記録しました。要点は次の3件です。

1. 継続指示の表記が「続きを書いて」（§4.8）と「続けて」（§14.1、§15）で揺れている。`SPEC.md`では両方を認識する仕様にした。
2. `memo.md`§5.3の`preferences/`は4ファイルだが、§3の嗜好分類は6種類ある。「反応から確認できた好み」「文脈依存の要素」に対応するファイルがない。
3. `memo.md`§5.3の`agents/`に、§5.1の役割一覧にある「意外性と納得感の監査」「事実確認」に対応するファイルがない。

いずれも`memo.md`側を修正すべきかはhantaniの判断です。修正が必要なら別途指示してください。

---

## Q7〜Q16 正式回答（`instructions/instruction-20260727-5.md`）

### Q7 正式回答: 選択肢1を採用

μITRON型と呼んでいた機能は、正式名称を「待機ポーリング型ワーカー」としてv0.5 MVPへ昇格する。第20章、`NFR-017`、`TBD-010`、受入条件、要求集計、トレーサビリティを実態と一致させる。

### Q8 正式回答: 選択肢3を採用

処理完了、既読、返信済みは別の状態・事実として管理する。`completed`になっても既読または返信済みとはみなさず、既存DB・CLI互換性を維持する。

### Q9 正式回答: 選択肢1を採用

`max_attempts`は初回を含む合計試行回数とし、既定値3とする。既存の`max_retries`は互換エイリアスとして扱い、両方を矛盾して指定した場合はエラーにする。README、CLI、テスト、仕様で意味を統一する。

### Q10 正式回答: 選択肢2を採用

強制停止を保証するのはワーカーが子プロセスとして起動する登録済みCLIプロセスとする。Pythonハンドラーは信頼済み内部用途とし、タイムアウト後もスレッドや副作用を完全には停止できない制約を明記する。`shell=True`は使用しない。

### Q11 正式回答: 選択肢2を採用

放置処理の回復にはリースまたはフェンシングトークンを使い、古いワーカーの後続DB更新を拒否する。ただし外部副作用は取り消せないため、ハンドラーにはメールIDまたは系列IDを重複排除キーとして使える契約を設け、冪等性の責任を分けて定義する。

### Q12 正式回答: 選択肢3を採用

CLIワーカーは未登録の対象エージェントを安全な既定値で自動登録できる。既存登録の属性が矛盾する場合は上書きせずエラーとする。ハンドラーの入力、出力、返信生成、例外、タイムアウト、冪等性、許可される副作用の契約を定義し、特定モデル・CLI・役割名へ固定しない。

### Q13 正式回答: 選択肢1を採用

返信・派生メールの系列ID、親ID、ホップ数は原則として親メールから自動導出する。親の不存在、系列不一致、ホップ数不正、循環参照、最大ホップ回避の偽装は拒否する。既存APIを残す場合も整合性を検証する。

### Q14 正式回答: 選択肢2を採用

本文最大長を設定可能な上限として実装し、超過時は切り詰めずエラーにする。新規DBは可能なOSで所有者のみ読み書きできる権限を設定し、既存DBの権限を無断で広げない。保存期間、アーカイブ、削除時期の具体値は`TBD-010`として未解決に残す。

### Q15 正式回答: 選択肢2を採用

正式名称は「待機ポーリング型ワーカー」とする。「イベント駆動」「常駐ワーカー」「μITRON型」は誤解を招く箇所では置き換え、設計思想を説明する場合も真のOSタスク機構やイベント通知を実装したように記述しない。

### Q16 正式回答: 選択肢1を採用

`AC-019`〜`AC-025`を正式なMVP受入条件とし、第19章、第21章、第23章、第24章、件数表、対応表、トレーサビリティを更新する。要求ID、受入条件ID、TBD IDの重複、欠番、未定義参照、件数不一致を機械的に検証する。

---

## `SPEC.md` v0.4 レビュー記録（正式回答済み）

以下は、`SPEC.md` v0.4と`mail/`の現実装を照合して見つかった不明点と、その後の正式回答である。選択肢は提起時の履歴として残し、正式回答は末尾のQ7〜Q16節に記録する。

### Q7. μITRON型ワーカーはMVPの実装範囲か

`SPEC.md`第20章8番は、イベント駆動ワーカー、処理予約、4状態、異常終了時の再試行、タイムアウトを「将来拡張」としている。一方、第24章はこれらをv0.4の実装済み範囲として記録し、AC-019〜AC-025を追加している。また、`NFR-017`は複数ワーカーの処理予約と異常終了後の再試行を「現行版の対象外」としている。

`mail/agent_mail.py`にはすでに実装があるため、要求上の扱いを確定したい。

**選択肢:**

1. μITRON型ワーカーをv0.4のMVPへ昇格し、第20章、NFR-017、TBD-010、受入条件件数、トレーサビリティを更新する
2. ワーカーは実験的な実装として残し、仕様上は将来拡張のままにする
3. 一部だけMVPとする（例: 排他取得・状態管理はMVP、タイムアウト・回復は将来）
4. 自由記入

### Q8. 処理完了と既読・返信の関係をどう定義するか

仕様は既読と処理完了を別概念とする一方、`AI-012`は受信メールを処理して返信保存と受信者既読化まで行う手順を定めている。現実装の`Worker`はハンドラー正常終了時に処理状態を`completed`へするが、返信保存や`recipient_read`の更新はハンドラー責務であり、返信なしでも完了できる。

ワーカーが扱う「正常終了」の完了条件を確定したい。

**選択肢:**

1. ハンドラーが処理結果を返せば`completed`。返信・既読は必要なハンドラーだけが行う
2. 受信メールは返信保存と受信者既読化まで成功して初めて`completed`
3. `completed`は処理状態、既読・返信は別状態として現状実装を正式仕様にする
4. 自由記入

### Q9. 最大再試行回数と既定値の意味

`WorkerConfig.max_retries`は既定値3で、取得時に試行回数を1増やす。現実装では、`attempt_count < max_retries`の間だけ`pending`へ戻すため、値3は合計3回の試行を意味する。仕様本文のTBD-010は再試行回数を未決としており、CLIでは任意値を受け付ける。

再試行回数の数え方と既定値を確定したい。

**選択肢:**

1. `max_retries`は合計試行回数とし、既定値3を仮仕様にする
2. `max_retries`は初回後の追加再試行回数とし、合計4回を仮仕様にする
3. 既定値は設けず、CLI・設定で必須指定する
4. 自由記入

### Q10. ハンドラーのタイムアウト後に残る処理をどう扱うか

Python関数ハンドラーはデーモンスレッドで実行しているため、タイムアウト後にワーカーが状態を失敗へ更新しても、ハンドラーのスレッド自体を強制停止できない。ハンドラーがその後にメール送信やファイル変更を行う可能性がある。CLIハンドラーは`subprocess.run`のタイムアウトでプロセスを終了できるが、任意のPythonハンドラーとは挙動が異なる。

タイムアウトの安全性をどこまで要求するか確認したい。

**選択肢:**

1. Pythonハンドラーは協調停止契約を持たせ、外部副作用は停止後に行わない
2. タイムアウト保証はCLIサブプロセスだけとし、Pythonハンドラーは協調停止できない場合があることを仕様へ明記する
3. Pythonハンドラーも別プロセスで実行し、強制終了可能にする
4. 自由記入

### Q11. 放置処理の回復と二重副作用の関係

`recover_stale`は一定時間以上`processing`のメールを`pending`へ戻す。しかし、異常終了と判定した元ワーカーが実際には動作を続けている場合、回復後の別ワーカーと同じメールを二重処理する可能性がある。現状は処理予約トークンやフェンシング世代番号を持たない。

放置処理の回復に必要な保証を確定したい。

**選択肢:**

1. `stale_after`経過後の再処理を許可し、ハンドラーは冪等であることを運用要件にする
2. ワーカーID・リース世代を持たせ、古いワーカーの完了更新と副作用を拒否する
3. 回復は手動確認後だけ許可し、自動ループでは実行しない
4. 自由記入

### Q12. CLIワーカーのエージェント登録とハンドラー契約

`SPEC.md`はDB初期化後に任意のエージェントを登録して利用開始すると記述する一方、現実装の`worker-once`／`worker-loop`は対象エージェントを自動登録する。また、CLIは指定コマンドへメール本文を標準入力で渡すが、終了コード以外の応答形式や返信方法は定義されていない。

CLIワーカーの前提条件を確定したい。

**選択肢:**

1. ワーカー実行前に対象エージェントとエスカレーション先を登録必須とし、自動登録をやめる
2. 対象エージェントだけは自動登録可、エスカレーション先は登録必須とする
3. 自動登録を正式仕様とし、表示名・権限・ハンドラーの入出力契約を追加定義する
4. 自由記入

### Q13. 系列情報の整合性をどこまで検証するか

`conversation_id`、`parent_message_id`、`hop_count`を保持するが、現実装の低レベル`send` APIは、指定された親が存在すること以外に、親子の送受信関係・系列ID一致・ホップ数の連続性を検証しない。`HandlerContext.send`では自動的に引き継ぐが、CLI/APIから任意値を指定できる。

系列情報の不正な手動指定を拒否するか確認したい。

**選択肢:**

1. 親メールの系列IDとホップ数から自動計算し、矛盾する指定を拒否する
2. 監査用途の手動補正を許可し、補正理由と実行者を記録する
3. 低レベルAPIでは検証せず、`HandlerContext`経由だけを正規経路とする
4. 自由記入

### Q14. メールの保存・削除・本文長・アクセス権をどう決めるか

`TBD-010`は保存期間、アーカイブ、削除方法、最大本文長などを未決としている。`NFR-018`はOS権限による保護を要求するが、`mail/agent_mail.py`はDBファイルのアクセス権を設定していない。また、SQLite本文長の上限、削除前の監査・バックアップ条件もない。

運用上の具体値と、実装が担う範囲を確定したい。

**選択肢:**

1. 保存期間・最大本文長・DB権限・アーカイブ手順を運用仕様として別途決める
2. DB作成時にOS権限と本文長を実装で強制する
3. 機密情報を扱わない暫定運用として、具体値を段階2まで保留する
4. 自由記入

### Q15. 「イベント駆動」とポーリングの扱い

第20章は「イベント駆動ワーカー」と表現するが、現実装の`worker-loop`は`poll_interval`ごとにSQLiteを確認するポーリング方式である。OS通知やSQLiteの待機通知は実装していない。

仕様上の用語を確定したい。

**選択肢:**

1. ポーリングでメール到着時だけハンドラーを起動する方式も「イベント駆動」と呼ぶ
2. 「待機ポーリング型ワーカー」へ用語を改め、真のイベント通知は将来構想にする
3. SQLiteの通知・ファイル監視など、待機中のポーリングを減らす仕組みを追加する
4. 自由記入

### Q16. v0.4の受入条件と要求ID集計をどう整合させるか

第24.3節は`AC-019`〜`AC-025`を追加しているが、第19.4節のMVP完了判定、第23.10節の件数表、既存のAC対応表はv0.3時点の件数を保持している。第24.5節では既存要求との対応を文章で示しているものの、23章の要求トレーサビリティ表へ新受入条件の対応が反映されていない。

文書構造の更新範囲を確認したい。

**選択肢:**

1. AC-019〜AC-025を正式なMVP受入条件として、19章・21章・23章の件数と対応表を更新する
2. AC-019〜AC-025は第24章の実装確認項目に留め、正式な受入条件IDは増やさない
3. ワーカー機能を段階2へ戻し、AC-019〜AC-025を将来拡張の検証項目にする
4. 自由記入

## 正史・台帳更新工程の未解決事項

実Geminiの台帳抽出・監査応答の運用検証は、認証情報を扱わず、利用可能な環境で別途実施する。
## 正史・台帳更新工程の実装記録（v1.2）

今回の実装では新しい利用者判断を追加していない。版付き5成果物、Gemini／ダミーの実行区別、承認前確定拒否、差戻し履歴、次章引き渡しをコードとテストで確認した。実Geminiの資格・応答検証は未実施であり、既存の未解決事項として保持する。
