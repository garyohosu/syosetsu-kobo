# 小説工房 共通エージェント作業規則

このファイルは、Claude Code、Codex、AntiGravity、Gemini CLIその他、このリポジトリを編集するすべてのAIエージェントに適用する共通指示です。個別の`instructions/instruction-*.md`により具体的な指示がある場合は、その指示を優先し、本ファイルは作業継続・Git運用・引き継ぎの共通基準として扱ってください。

## 1. Gitを引き継ぎの正本にする

- 別のAIとの会話履歴や、前のセッションだけに残る説明へ依存しないでください。
- 作業開始時に、最低限次を確認してください。

```powershell
git status --short --branch
git remote -v
git log --oneline --decorate -10
```

- `AGENTS.md`、対象の`instructions/instruction-*.md`、対応する`result-*.md`、`hikitsugi.md`が存在する場合は、実装開始前に読んでください。
- 完了済み内容をコミット履歴と結果報告で確認し、同じ実装を最初からやり直さないでください。
- 次のエージェントがGitだけを見て、安全に続行できる状態を作ってください。

## 2. 同じ作業ツリーを複数エージェントで同時編集しない

- Claude Code、Codex、AntiGravity、devloop等を、同じ作業ツリーに対して並行して書込み実行しないでください。
- 別のエージェント、devloop、テスト、Git処理が書込み中と判断できる場合は、新しい編集を開始せず状態を確認してください。
- 同時作業が必要な場合は、専用ブランチまたは専用worktreeを使ってください。
- 他の作業者による未コミット変更を、許可なく削除、上書き、stash、reset、checkout、cleanしてはいけません。

## 3. 利用枠・トークン残量が少ない場合の必須動作

次のいずれかに該当した時点で、新しい大きな工程へ着手しないでください。

- 利用枠、トークン、コンテキスト等の残量が25%以下と表示された
- rate limit、session limit、quota、利用上限に関する警告が出た
- 現在のセッションが途中で強制終了する可能性が高い
- 残り時間や実行枠では、次の工程を検証まで完了できないと判断した

この場合は、次のチェックポイント処理を優先してください。

1. 現在着手中の変更を、可能な範囲で最小の整合した単位まで仕上げる。
2. 新しい機能、別タスク、大規模リファクタリングには着手しない。
3. 対象テストを実行する。
4. 実行可能なら全テスト、`compileall`、`git diff --check`も実行する。
5. 完了済み、未完了、未検証、次に行う正確な作業を結果報告または`hikitsugi.md`へ記録する。
6. 現在の指示でコミットまたはpushが明示的に禁止されていない限り、チェックポイントをコミットし、リモートへpushする。
7. pushできない場合は、ローカルコミットを作成し、ブランチ名、コミットSHA、pushできなかった理由を報告する。
8. 作業ツリーを可能な限りクリーンにして終了する。

## 4. チェックポイントコミット

- 通常は、テスト可能で意味のある小さな単位ごとにコミットしてください。
- コミットメッセージは、実際の状態を正確に表してください。
- 受入条件を満たしていない実装を`feat:`や`fix:`で完了扱いしてはいけません。
- 利用枠切れ等により未完成状態を保存する必要がある場合は、内容を失わないことを優先し、`wip:`であることを明示してください。
- WIPコミットには、対応する結果報告へ次を記録してください。
  - 完了した部分
  - 未完了の部分
  - 実行したテスト
  - 実行していないテスト
  - 既知の問題
  - 次の具体的な作業
- force push、履歴書換え、既存コミットの無断修正は禁止です。

## 5. pull・rebase・push

- push前に`git fetch`し、リモート先端を確認してください。
- ローカル変更をコミット済みで、通常の履歴統合が必要な場合は、現在の作業指示で禁止されていなければ`git pull --rebase`を使用して構いません。
- 競合が発生した場合は、内容を確認せず機械的に解決しないでください。競合箇所と判断事項を報告してください。
- リモート側と無関係な変更しかない場合でも、未コミット変更を残したままpullしないでください。
- push成功後は、ローカルブランチと追跡リモートの同期状態を確認してください。

## 6. 結果報告

各作業の終了時、または利用枠低下による引き継ぎ時には、最低限次を報告してください。

- 対象タスク・指示書
- 変更ファイル
- 実装済み範囲
- 未実装・未解決事項
- テスト結果と件数
- 実行していない検証
- コミットSHA
- push結果
- 現在ブランチ
- `git status --short --branch`の要約
- 次のエージェントが最初に実行すべき具体的な操作

失敗、利用枠切れ、外部プロセス終了を、実装完了として記録してはいけません。途中成果物が存在する場合は、それを失敗理由と区別して保存してください。

## 7. 完了判定

- テストが通っただけで、指示書の受入条件を満たしたと推測してはいけません。
- 指示書の各必須項目、成果物、テスト、文書更新を照合してください。
- 完了していない場合は`completed`や「完了報告」と記載せず、`blocked`、`stopped`、`partial`、`wip`等、実態に合う状態を使ってください。
- 次のAIが誤って完了扱いしないよう、正式結果と途中報告を明確に分けてください。
