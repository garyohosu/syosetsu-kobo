# Dreaming記録

## 2026-07-27 14:26 Dreamingタイム

### 今回やったこと
- `git pull`で新規資料5件を取得（`instructions/instruction-20260727-1.md`、`MyLike.md`、`MyLike_kousatsu.md`、`instruction-create-SPEC.md`、`memo.md`の大幅追記）。
- 指示書に従い、`memo.md`（866行）を主資料、補助資料4件を根拠確認用として`SPEC.md` v0.1を新規作成。全23章、要求ID 130件、受入条件 14件、未決事項 9件。
- hantaniの判断が必要な確認事項6件を`QandA.md`へ選択式で整理（`SPEC.md`第22章とTBD-IDで相互参照）。
- 完了報告書を`instructions/result-20260727-1.md`へ作成。

### 気づいたこと
- 同一内容の指示書が2箇所にあった（`instruction-create-SPEC.md`とリポジトリ直下、`instructions/instruction-20260727-1.md`）。差分は§12と§14のみで、後者が結果報告書の作成を追加した上位互換。日付・連番付きの`instructions/`配下を正とするのが安全。
- `memo.md`§16の「ChatGPTとの壁打ち」と§5.1の「工房長はClaude Code」が衝突して見える。§16.1〜16.7が全部システム内部工程として書かれているので内部実装と読んだが、ここはMVP範囲を左右するので`QandA.md` Q1へ上げた。
- `memo.md`が自ら「案」「初期案」「実装段階で見直す」と書いている箇所（§7の停止条件、§5.3のディレクトリ構成）は、要求と実現方法を分ける格好の材料だった。要求は「停止条件を持つこと」「5種を混ぜないこと」で、具体値は仮仕様。
- `MyLike_kousatsu.md`の「必須候補」「強く推奨する要素」は、そのままFR化すると本人未確認の推定を確定要求に格上げしてしまう。要求（仕組み）とデータ（嗜好モデルの中身）を分ける線引きが一番の落とし穴だった。
- 未追跡ファイルに`git diff --check`をかけても何も検査されない。`git add -N` → check → `git reset` で実際に検査させた。
- 自己レビューで3件の抜けを検出した。(1) 第10章の各工程に「入力」「出力」が明示項目として無かった（指示書§4の「各工程の入力、出力」を満たしていなかった）。(2) 10.4／10.6／10.8に「失敗時の扱い」が欠落。(3) AC-002の段階が「安定化」なのに対応要求は全部MVPという矛盾。いずれも本文だけ読むと自然に見えるので、機械的な観点チェックが要る。

### 改善点
- `QandA.md`と`SPEC.md`第22章の役割分担を、書き始める前に決めておいたのは正解だった。後から分けようとするとコピペ文書になる。
- 要求IDを先に振ってから本文を書くより、「根拠となる`memo.md`の節を言えないものは要求にしない」という縛りを先に置いたほうが、出典のない仕様が混入しない。
- ID参照の整合は目視ではなく正規表現で機械的に検査した（130件、欠番・未定義参照なし）。この規模の仕様書では必須。
- ただし最初の検査は「自分で数えた上限値と照合する」だけで、トレーサビリティ表にしか現れず本文で定義されていないIDを見逃す穴があった。行頭の定義箇所を抽出して参照集合と突き合わせる形へ直した。検査スクリプトが何を検査していないかを確認するところまでが検査。
- 「書式は問題なし」と報告する前に、実際にコードフェンスの開閉数と表のパイプ数を検査した。目視で済ませた判定を検査したと書かない。

### 次に試すとよさそうなこと
- `QandA.md` Q1〜Q3（対話相手、承認単位、`MyLike.md`の聞き取り方法）の回答を受けて`SPEC.md`を v0.2 へ更新する。
- `SPEC.md`承認後、`spec-to-design` skillで`USECASE.md`／`SEQUENCE.md`／`CLASS.md`／`UI.md`／`TESTCASE.md`を起こす。状態遷移（STATE-002）はMermaid図が効くはず。
- `MyLike_kousatsu.md`の「URS作成時に優先して尋ねる質問」10項目は、そのまま対話型URSメーカーの初期質問キューの雛形になる。実装前に選択式へ変換して試す価値がある。
- 停止条件（TBD-004）と確信度の尺度（TBD-005）は実測が要る。MVPを一巡させてから決める。

## 2026-07-28 09:19 Dreamingタイム

### 今回やったこと
- `hikitsugi.md`に従い、開発ループ実地試験を再開。既知のブロッカー（`codex exec`の`--ask-for-approval`）を`devloop.json`から削除した。
- 追加で2件のブロッカーを発見・修正。(1) `devloop.json`の`tests`が使う`python`がMicrosoft Storeのスタブ（`exit=9009`）だったため`py -3`へ変更。(2) `_run`が`text=True`のみ指定していたためcp932で復号され、日本語UTF-8出力が無言で消えていた問題を`default_runner`で`encoding="utf-8"`／`errors="replace"`を明示して修正。回帰テスト3件を追加（110→113件、全件成功）。
- `devloop-once --execute`を実行し、実装→テスト→差分→レビュー→revise→再実装の連鎖が動くことを確認。ただしジョブは`blocked`で停止。
- `hikitsugi.md`を現状に合わせて全面更新。

### 気づいたこと
- 一番厄介だったのは3件目。`UnicodeDecodeError`はreader thread内で起き、**returncodeは0のまま`stdout`だけが空文字列になる**。エラーが出ないので「レビューAIが毎回reviseと言う」という遠い症状としてしか現れない。`diff-N.patch`が0バイトなのに`git diff`単体では6000文字出る、という矛盾から辿り着いた。
- 症状（reviseの繰り返し）と原因（subprocessの復号設定）が3層離れていた。中間成果物のサイズを直接見に行ったのが早道だった。
- 最終的な停止理由は不具合ではなかった。実装AIは指示書§2「作業ツリーがクリーン」とL37「未コミット変更を発見したら停止して報告」に正しく従って止まっていた。生成された`result-20260727-12.md`も全項目「未実施」と正直に書かれており、虚偽の完了報告ではない。
- 自分の修正が作業ツリーを汚し、それが実装AIの停止条件を踏む、という循環になっている。ループを進めるにはコミットが要るが、引き継ぎ方針は「コミットしない」。ここは利用者の判断事項。

### 改善点
- PowerShellの`Get-ChildItem`の表形式出力は遅延フラッシュされるため、テキスト行と混ぜると出力順が入れ替わる。一瞬「AIが作業ツリーを巻き戻したのでは」と誤読しかけた。状態確認は1コマンド1目的で出すべきだった。
- `.kobo/`はgitignoreされているので、前回のblockedジョブDBは残っていなかった。引き継ぎに書かれた「既存blockedジョブの再利用を確認する」は実施できず、代わりにユニットテストで`_claim_job`の挙動を確認した。引き継ぎには「gitignore対象の状態は次セッションに残らない」と書いておくとよい。

### 次に試すとよさそうなこと
- 未コミット変更の扱い（コミットするか、別ブランチか、stashか）を決めてから`devloop-run --execute --max-cycles 3`を再実行する。
- `instructions/result-20260727-12.md`を削除または改名しないと、`discover()`がinstruction-12を完了扱いにして二度と拾わない。再実行前に必ず対処する。
- `_run`が返す文字列は現在エラー時に捨てられている。`KoboError`へstdout/stderrの末尾数百文字を含めると、今回のような遠い症状の切り分けが速くなる。
- 外部コマンド出力を扱う他の箇所（`kobo/gemini.py`など）にも同じcp932問題がないか確認する。

## 2026-07-29 10:30 Dreamingタイム

### 今回やったこと
- `instruction-20260729-01.md`（`instruction-20260728-21.md`の再実行指示）を確認したが、`git log`上は前日のコミット`4570400`で既にeditorial-board-v002が完成済みだった。AGENTSの「やり直すな」原則に従い、まず再実行ではなく検証から入った。
- 検証の結果、旧v002が主張する「実Antigravity生成（session_id=concept-08010bc4c14941b19a3896b3efb45582）」がプロジェクトの実DB（`.kobo/state.db`）に一切記録されていないことを発見した。`ConceptManager.start()`はdummy/agyを問わず必ず`concept_sessions`へ行を挿入するコードなのに、そのセッションIDが存在せず、`runs`テーブルは総行数0件だった。
- 利用者に状況を報告し、再生成の許可を得た上で、旧v002を`editorial-board-v002-unverified`へ改名保存し、新しい実セッション（`concept-169e90deba774338951b29d14b076073`）でv002を作り直した。1回目のセッションはC01が3回とも文字混入で失敗し正しく停止、2回目で5案とも成功。
- 結果報告書を全面的に書き直し、検証手順と発見内容を記録した。テスト・compileall・git diff --check・agy-doctor・agy-smokeを全て再実行し成功を確認。

### 気づいたこと
- 「テストが全部通る」「ファイル内容が要件を満たしている」は、その成果物が本当にAI経由で生成されたことの証明にはならない。今回のように、静的ファイルとしては完璧に見える偽装（あるいは出所不明の生成物）が紛れ込む余地がある。
- 真正性を確かめる最も確実な方法は、コード側が必ず書き込む一次記録（今回は`concept_sessions`テーブルへのINSERT）を、成果物の主張（PROVENANCE.jsonのsession_id）と突き合わせることだった。ファイルの体裁がどれだけ整っていても、DBの裏付けがなければ「検証不能」として扱うべきだった。
- 実際に本物のAntigravityを走らせると、「修復専用プロンプトで指摘箇所だけ直す」よう指示しても、直した拍子に別の箇所へ同種の混入（`of`）を新たに作ってしまうことがあった。これはコードの不具合ではなくモデルの実際の挙動であり、ダミー代替を禁止する設計のおかげで不正な成果物が正式保存される前に止まった。

### 改善点
- 「前回セッションの成果物」を信頼する前に、コードが必ず書き込むはずの一次記録（DB行、ログ、runファイルなど）が実在するかを先にチェックする習慣を、今後も最初のステップに入れる。
- 疑わしい発見をした時点で黙って再生成せず、一度利用者に状況と選択肢を提示してから進めたのは良い判断だった（AskUserQuestionで確認してから実行）。

### 次に試すとよさそうなこと
- 過去のコミット（`87a2eba`、`8b5cc76`など、他の「実Antigravity」を主張する成果物）についても、同様に`.kobo/state.db`との突き合わせで真正性を再検証する価値があるかもしれない。ただし`.kobo/state.db`はGit非追跡でローカルにしか残らないため、当時の記録が既に失われている可能性が高く、検証できるとは限らない。
- `PROVENANCE.json`の`session_id`を、公開時に自動でDBと突き合わせて検証するテストを`tests/test_concept.py`へ追加すれば、今回のような食い違いを次回から自動検出できる。

## 2026-07-29 訂正: マシン固有DBによる真正性判定は誤りだった

### 訂正内容

上の「2026-07-29 10:30 Dreamingタイム」で記録した結論を撤回する。旧`editorial-board-v002`を「実Antigravity生成であることを検証できない」と判定したのは誤りだった。

`.kobo/`は`.gitignore`によりGit非追跡であり、`state.db`は**生成を実行したマシン固有の状態**である。別の作業環境から照合しても記録が無いのは当然であり、それは生成されなかった証拠にならない。

生成マシンで照合したところ、`concept_sessions`のsession_id、候補5件の`generation_run_id`、候補タイトル、`attempts/`の生出力がすべて一致した。逆に、再生成セッション`concept-a955f312...`と`concept-169e90de...`はこのマシンのDBに存在しない。同じ判定方法を適用すれば再生成側も検証不能になってしまい、方法そのものが成立していない。

`runs`テーブル総行数0件を証拠とした点も撤回する。**ConceptManagerは企画生成runを`runs`テーブルへ保存しない。** `concept_candidates.generation_run_id`と`concept_evaluations.evaluation_run_id`へ保存するため、`runs`の件数は企画生成の真正性を示さない。

上の節の「次に試すとよさそうなこと」にある「`PROVENANCE.json`のsession_idを公開時にDBと突き合わせて検証するテスト」は、**同一環境内でのみ**有効である。CIや別マシンでは常に失敗するため、そのまま実装してはいけない。

### 維持する一般論

- Git追跡成果物だけでは、実際に外部モデルが呼び出されたことを完全には証明できない。
- ローカルDBとの照合は、**同じ作業環境内での**整合性確認には使える。
- 任意の別マシンのローカルDBとの不一致を、生成されなかった証拠として使ってはいけない。

### 改善候補: 携帯可能な実行証跡（未実装。記録のみ）

別マシンでも整合性を確認できるよう、`concept-publish`時にGit追跡可能な実行証跡（例: `EXECUTION_TRACE.json`）を出力する案。C01のストーリーバイブル工程を止めてまで実装するものではない。

記録する候補項目:

- session情報（session_id、work_id、status、開始・終了時刻）
- `concept_candidates`の候補ID、run ID、タイトル
- `concept_evaluations`の評価run ID
- adapter、model、CLI版
- 入力プロファイルの相対パスとSHA-256
- 候補・評価・比較総括・raw attemptのSHA-256
- 出力日時
- 証跡を生成したstate.dbのスキーマ版

含めないもの: 絶対パス、ユーザー名、認証情報、プロンプト内の機密情報。

**表現上の注意:** これは「成果物と実行記録の整合性を携帯可能にするもの」であり、外部モデル呼び出しを暗号学的に証明するものではない。証跡自体も成果物と同時に書けてしまう以上、真正性の証明とは書かないこと。用途は、別環境へ引き継いだときに「この成果物はどのsession・run・入力に対応するか」を追跡可能にすることに限る。

## 2026-07-29 夕 C01確定とストーリーバイブル工程

### 今回やったこと
- 前セッションの「旧v002はprovenance検証不能」という判定を撤回した。マシン固有のstate.dbを別環境から照合していたことが誤判定の原因で、生成マシンではsession・候補run ID・タイトル・raw attemptがすべて一致した。
- instruction-20260729-02を実行しようとしたところ、§4/§8/§10が固定指定する`concept-169e90de...`がこのマシンのDBに存在せず、正規操作が実行できなかった。DB行を捏造せず利用者へ判断を仰ぎ、「後継セッション」を作る方針を得た。
- C01_REVISION.v001.mdを作成し、v003を入力に候補1件の実agyセッションを生成、C01を実AIで改訂して仮題「温かいスープと黒猫の薬草小屋」を復元、select→revise→preview→finalizeでCONCEPT.v001.mdを確定した。
- ストーリーバイブル草案と独立監査を実agyで生成し、bible_awaiting_approvalで停止した。

### 気づいたこと
- **`concept-revise`はAIを一切呼んでいなかった。** 指示ファイルを記録するだけで、指示書が期待する「改訂run ID」「adapter=agy」「文字品質検証」を満たせる実装が存在しなかった。指示書がコマンドを名指ししていても、その中身が期待通りとは限らない。実行前に実装を読む必要がある。
- **`story_design._audit()`も非ダミーで定型文を返すだけだった。** 「独立監査を実行した」と報告できる状態ではなかった。§15が「DB行、run成果物、adapter記録で確認できた場合だけ独立監査と呼べ」と釘を刺していたのは正しい。
- **agyはエージェント型CLIである。** 簡素なタスク指示を与えると、自分のワークスペースへファイルを書き、標準出力にはチャット形式の報告を返す。企画工程が動いていたのは「ファイルを作らない」「標準出力へMarkdown本体のみ」と明示していたからで、story_designにはその明示がなかった。同じアダプターでも、プロンプトの書き方で成否が変わる。
- 実AIの独立監査は定型文と違い、実際に有用な指摘を返した。黒猫の名前「ルナ」が既存の著名キャラクターと重複する模倣リスクは、こちらが気づいていなかった指摘だった。

### 改善点
- 「正規パイプラインで実行した」と報告する前に、そのコードパスが本当に外部モデルを呼んでいるかを実装で確認する。関数名や CLI 名だけで判断しない。
- 生成が失敗したとき、まず生の出力を読む。今回は13見出しすべて欠落という結果から、モデルが別の成果物（自分のワークスペースのファイル）を作っていたことがすぐ分かった。

### 次に試すとよさそうなこと
- 携帯可能な実行証跡（EXECUTION_TRACE.json）の実装。今回の§4実行不能は、これがないことが直接の原因だった。ただし証跡自体も成果物と同時に書けるため、真正性の証明ではなく追跡可能性の担保として位置づける。
- `_dummy()`が返す定型文と、実AI経路の出力を、テストで明確に区別できるようにする。今回`_audit`が定型文しか返していなかったことは、既存テストでは検出できなかった。
