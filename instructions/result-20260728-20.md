# instruction-20260728-20 結果報告

## 実セッションID

`concept-4e6d3d9accf944a0b60d6012f7e2b284`（work_id: `prototype-001`、状態`awaiting_selection`）

## adapterとmodel

| 項目 | 値 |
|---|---|
| adapter | `agy`（実Antigravity） |
| agy CLI | 1.1.8（`agy-doctor`、`agy-smoke`ともに成功） |
| model | `Antigravity既定（--model未指定）` |
| dummy | false |

`configured-by-settings`は`agents/planner.md`の設定値そのものですが、`AgyAdapter`はこの値のとき`--model`を渡しません。実際にはAntigravity既定モデルが使われるため、成果物には`Antigravity既定（--model未指定）`と表示しています。

## セッションの経緯（削除・上書きなし）

| セッション | adapter | 状態 | 備考 |
|---|---|---|---|
| `concept-1faf945e…` | dummy | superseded | 新基準の最初のダミー |
| `concept-6e270711…` | dummy | superseded | 利用者向けだったダミー5案。指示どおり履歴として保持 |
| `concept-4e6d3d9a…` | **agy** | **awaiting_selection** | **実Antigravityの5案。利用者はこれを読む** |

instruction-18時点の旧5案（`novels/prototype-001/CONCEPTS.v001.md`）も未変更です。

## 各候補の仮題、ログライン、文字数

| 候補 | 仮題 | ログライン字数 | あらすじ字数 | 中心人物 | 生成attempt |
|---|---|---:|---:|---:|---:|
| C01 | 合理的すぎる二人のビジネス婚約 | 78 | 729 | 3 | 1 |
| C02 | 僕の影は、君の本音を見逃さない | 79 | 638 | 3 | 2 |
| C03 | 遺品整理士エレナの静かなる解読録 | 78 | 592 | 3 | 1 |
| C04 | 限界測量サバイバル | 59 | 634 | 3 | 2 |
| C05 | 磁力猫と学者カイルの境界開拓日誌 | 64 | 570 | 3 | 1 |

ログラインは全案80字以内、あらすじは全案500〜800字、中心人物は全案3人以内です。方向指定（C01恋愛／C02不思議な日常／C03秘密と約束／C04冒険と危機／C05猫が中心）どおりに分かれています。

## 生成attempt数

候補生成の実呼出しは7回（5案 = 成功5 + 書式不合格2）。C02とC04が1回目に不合格となり、検証エラーを次のプロンプトへ渡して2回目で合格しました。不合格attemptは`attempts/`に`.md`と`.error.txt`として残しています。ダミーへの代替は一切していません。

## 評価・比較の実行回数

- `concept-reviewer`による独立評価: 5回（全案とも1回目で10軸検証に合格）
- 比較総括: 1回

全評価に10軸すべてと、各軸の「根拠／長所／弱点／改善案／5段階評価」があります。推奨順位は候補番号順ではなく、各軸の5段階評価の合計から導出しています。

| 順位 | 候補 | 合計点 |
|---:|---|---:|
| 1 | C05 | 39 |
| 2 | C01 | 37 |
| 3 | C04 | 37 |
| 4 | C03 | 35 |
| 5 | C02 | 30 |

比較総括のAI順位（C03 → C01 → C05 → C02 → C04）は、この点数順位とは異なります。どちらも補助情報であり、自動選択には使いません。

## 編集会議HTMLのローカルパス

`.kobo/works/prototype-001/concepts/concept-4e6d3d9accf944a0b60d6012f7e2b284/editorial-board/index.html`

## Git追跡版の相対パス（利用者が最初に開くファイル）

```text
novels/prototype-001/editorial-board-v001/index.html
```

指示書§10の例は`editorial-board-v002`でしたが、`novels/prototype-001/`に既存の`editorial-board-*`が無かったため、未使用の最小版として`v001`を確保しました。既存版がある場合は上書きせず次の未使用版を取る実装です。

候補5件、評価5件、比較総括、`PROVENANCE.json`を同梱しています。絶対パス、ユーザー名、認証情報、外部通信、画像、JavaScriptは含みません。

## 実装内容

- `adapter`欄の是正: 非ダミーセッションを`gemini`と記録していた旧実装を`agy`へ修正。
- ダミー候補の選択禁止: `adapter=dummy`のセッションで`select`/`revise`/`preview`/`finalize`/`publish`をコードで拒否。`status`/`list`/`show`/`compare`/`board`/`hold`/`reject_all`/`regenerate`は許可。判定は表示ではなくDBの`adapter`で行う。
- 出自表示: 候補・評価・比較・HTMLへ生成種別、アダプター、モデル、セッションID、実行IDを表示。
- 方向固定と差別化: 候補番号ごとの創作方向をプロンプトへ明記し、C02以降は既に合格した候補のログライン・一行コンセプト・中心人物をプロンプトへ渡す。
- 限定再試行: 実AI出力を`attempts/`へ受け、検証合格分だけ正式候補へ原子的保存。失敗内容を次プロンプトへ渡し、上限超過時はダミーへ代替せず`failed`で停止。
- 実評価: `concept-reviewer`を`agy`で実行（`adapter`を`dummy`から`agy`へ、timeoutを60秒から300秒へ変更）。10軸・5段階評価を検証し、合計点から順位を導出。比較総括を1回だけ生成。
- `concept-publish`とCLIコマンドを追加。

## テスト結果

- `py -3 -m unittest tests.test_concept`: 32件成功
- `py -3 -m unittest discover`: 152件成功、1件スキップ
- `py -3 -m compileall -q kobo mail tests`: 成功
- `git diff --check`: 成功
- `py -3 -m kobo.cli agy-doctor`: 成功（agy 1.1.8）
- `py -3 -m kobo.cli agy-smoke`: 成功

追加テスト: 非ダミーsessionのadapterが`agy`／ダミーsessionのselect・revise・preview・finalize・publish拒否／ダミーsessionのboard・hold・reject・regenerate許可／実AI生成経路がplanner adapterを呼ぶ／候補番号ごとの方向指定がプロンプトへ入る／C02以降のプロンプトに既生成候補の要約が入る／検証失敗時の限定再試行／最大失敗後にダミーへfallbackしない／非ダミー評価がconcept-reviewer adapterを呼ぶ／非ダミー評価が固定定型文でも候補番号順位でもない／編集会議HTMLに生成種別が表示される／Git追跡用成果物に絶対パスが入らない。

`tests/test_manuscript.py`と`tests/test_story_design.py`は、ダミー企画セッションを`finalize`して後工程の前提を作っていたため、新しい拒否規則に合わせて実AI相当のスタブアダプターを使う形へ修正しました。

## 未解決事項

1. **C03のあらすじにAI由来の誤記があります。** 「木箱 of 底から現れたのは」と英単語が混入しています。評価担当は候補本文を書き換えない規約のため、こちらでも修正していません。C03を採用する場合は修正が必要です。
2. **主人公欄の「立場」検証が当初厳しすぎ、実AI出力を2回誤って却下しました。** 「遺品整理人」「登山案内人」のように職業語尾の一覧に無い語が原因です。`立場:`と明示されていれば受理する条件を追加し、`案内人`等の複合語も一覧へ足し、プロンプト側でも箇条書きでの明示を要求するよう修正済みです。ただし語尾一覧による推定である以上、未知の職業名で誤却下が起きる可能性は残ります（再試行で回復します）。
3. **モデル名が固定できていません。** `--model`を渡していないため、Antigravity側の既定モデルに依存します。特定モデルへ固定したい場合は`kobo.json`の`models`へ明示が必要です。
4. **出版版番号が`v001`です。** 指示書の例示は`v002`でした。既存の`editorial-board-*`が無かったための採番です。
5. 途中でセッションを中断した影響で、C01〜C03の出自ヘッダーだけ後から再生成しました。9見出しの創作内容とタイトルが一字も変わらないことをアサートしたうえで書き戻しています（AI生成内容そのものは未編集）。

## 本文・画像について

本文、ストーリーバイブル、挿絵、公開用HTML、note素材は作成していません。作成したのは企画ラフ5案、独立評価5件、比較総括、編集会議HTML、`PROVENANCE.json`のみです。利用者の選択・確定はまだ行われておらず、状態は`awaiting_selection`です。
