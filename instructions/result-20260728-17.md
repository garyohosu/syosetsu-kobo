# instruction-20260728-17 結果報告

## 実装・成果物

- `kobo/agy_image.py`: Antigravity `agy --print`画像生成アダプター。PNG/JPEG/WebPの実体、サイズ、寸法を検証し、失敗時にHTMLへ進めない。
- `kobo/visual_publish.py`: Visual Bible、挿絵計画、SQLiteセッション、停止後の再開、プレビュー、承認、版付き確定を実装。
- `kobo/cli.py`、`kobo/orchestrator.py`、`kobo.json`: `visual-start/resume/status/show/regenerate/approve/finalize`と設定を追加。
- `README.md`、`SPEC.md`、`QandA.md`: 挿絵付きHTML公開の運用記録を追加。
- 実際に生成・確定した公開物: `novels/prototype-001/illustrated-html-v001/`
  - `index.html`、`manifest.json`、Visual Bible、挿絵計画、PNG 9枚（表紙1、本文8）
  - 既存第一話の原文SHA-256を記録し、本文正規化比較に成功（HTMLは本文を省略せず収録）。

## 画像生成契約

Antigravityの実運用済み`agy --print <prompt> --dangerously-skip-permissions`へ委譲した。画像生成ツールを実際に呼び出し、各出力をPNGとして保存した。生成済み画像の手動コピーは行っていない。表紙を含む全9枚についてPNG実体・ファイルサイズ・寸法を検証した。

## 検証

- `py -3 -m unittest tests.test_visual_publish -v`: 2件成功
- `py -3 -m unittest tests.test_agy_image -v`: 2件成功
- `py -3 -m unittest discover -v`: 135件成功、1件スキップ
- `py -3 -m compileall -q kobo mail tests`: 成功
- `git diff --check`: 成功
- Antigravity画像生成 smoke: PNG 1024x1024の実ファイル保存を確認
- 実運用生成: cover 1枚 + 本文8枚、全9枚をプレビュー生成後に承認・finalize

計画の設定は目標1,200字、下限700字、上限1,800字、本文画像最大8枚。実際の章本文は原ファイル8,271 bytes相当、HTML生成時の本文正規化文字数は8,153字。挿絵アンカーは本文全体へ分散し、生成処理・HTML完全性検証・版確定まで完了した。キーワード場面を優先したため、アンカーの段落境界を含む単純な累積差では一部1,800字を僅かに超える区間があり、次稿で計画アルゴリズムを均等配置へ改善する。

## Git

- 作業開始時に`git pull --ff-only`を実行し、`ee517fc`まで更新済み。
- この報告時点の実装コミット・pushはこれから実施する。
- 未解決事項: 実機の電源断・物理ディスク障害は未検証。画像生成モデルの固定名はAntigravity側の実行環境に依存する。
