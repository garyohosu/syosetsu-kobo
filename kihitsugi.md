# 次にやる作業

## 題目

確定本文から正史・各種台帳を更新し、次章へ制作状態を引き継ぐ工程の実装

## 目的

承認・確定済みの`CHAPTER-NNN.vNNN.md`だけを入力として、作品の正史と継続制作に必要な台帳を更新する。未承認の初稿・監査・改稿を正史へ混入させず、次章の設計担当が承認済み状態だけを参照できるようにする。

## 実装内容

1. 確定本文から、次の版付き成果物を生成する。
   - `CANON.vNNN.md`: 確定した出来事と世界状態
   - `CHARACTER_LEDGER.vNNN.md`: 人物の状態、関係、知識、目的
   - `TIMELINE.vNNN.md`: 出来事の時系列
   - `RESOURCE_LEDGER.vNNN.md`: 所持品、能力、資源、制約
   - `FORESHADOWING_LEDGER.vNNN.md`: 伏線の設置、進行、回収状態
2. 台帳更新担当と独立整合性監査担当を分離する。
3. 確定本文、確定バイブル、確定プロット、直前の確定台帳を固定参照する。
4. 監査では人物、関係、知識、時系列、資源、能力、伏線、世界ルールの矛盾を確認する。
5. AI監査だけで正史更新を確定せず、利用者の明示承認後に限り版付き成果物を確定する。
6. 初回は空の台帳から開始し、第2章以降は直前の確定版を継承する。
7. 中断再開、実行ID、メール系列、非上書き保存、二重確定拒否を実装する。
8. 確定後は次章の`scene-planner`へ、確定プロット・バイブル・正史・各台帳のパスを引き渡す。
9. JSON CLI、決定的な`--dummy`工程、自動テスト、README、SPEC、結果報告を追加する。

## 安全条件

- 未承認本文、監査指摘、破棄された改稿を正史へ取り込まない。
- 上流資料や過去の確定台帳を上書きしない。
- 不明な設定を推測で確定せず、未解決事項として停止または承認待ちにする。
- Geminiが必要な創作文章工程で利用不能な場合、別AIへ無言で切り替えない。
- 長文本文をコマンドライン引数へ載せず、ファイルパス・実行ID・メールIDで参照する。
- Git競合、認証失敗、仕様矛盾、破壊的変更が必要な場合は停止する。

## 開発ループによる実地試験

この内容を正式な未処理`instruction-*.md`へ展開した後、まず公開なしで実行する。

```powershell
python -m kobo.cli devloop-run --execute --max-cycles 3
```

実装、テスト、独立レビュー、修正再依頼、result生成、次instruction生成が正しく連鎖したことを確認してから、必要に応じて`--publish`付きで実行する。

## 完了条件

- 確定本文だけから正史と5種の台帳を生成・監査・承認・確定できる。
- 次章が直前の承認済み状態を固定参照できる。
- 中断再開しても完了済み工程を重複実行しない。
- 既存のURS、企画、バイブル、プロット、本文、メール、開発ループの全回帰テストが成功する。
